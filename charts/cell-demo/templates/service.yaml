apiVersion: v1
kind: Service
metadata:
  name: {{ include "cell-demo.fullname" . }}
  labels:
    {{- include "cell-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cell-demo.selectorLabels" . | nindent 4 }}
